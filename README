We want to build a web based TicTacToe that uses the S3 architecture.
This will help us to create another show case, but more importantly it allows us to understand the underlying development model better.

This project is realeased under a MIT licence.
